# Chezmoi configuration repository

## How to use

* Install chezmoi

* Init the repository

    ```
    chezmoi init git@gitlab.com:kien-truong/chezmoi-config.git
    ```

* Apply the configuration

    ```
    chezmoi apply
    ```
